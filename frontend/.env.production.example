# Accounting frontend production environment for hiddenoasis.app.
# Copy to /etc/hiddenoasis/accounting-frontend.env and use the same values when running npm run build.

NODE_ENV=production
PORT=3100
NEXT_PUBLIC_API_BASE=/api
